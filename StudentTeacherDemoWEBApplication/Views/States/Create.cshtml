@model StudentTeacherDemoWEBApplication.Models.State

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>State</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StateName" class="control-label"></label>
                <input asp-for="StateName" class="form-control" />
                <span asp-validation-for="StateName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="country.CountryName" class="control-label"></label>
                <select id="CountryDropdown" asp-for="country.CountryName" class="form-control">
                    <option value="">Select a category</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {

    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script src="~/js/site.js"></script>
    <script src="~/js/dropdownlist.js"></script>

   @* 
    <script>
        $(document).ready(function () {
            // AJAX request to fetch data and populate the dropdown
            $.ajax({
                url: '/States/GetCountry', // Adjust the URL to match your route
                type: 'GET',
                success: function (data) {
                    var dropdown = $("#CountryDropdown");
                    dropdown.empty();
                    dropdown.append($('<option>').val('').text('Select a category'));
                    $.each(data, function () {
                        dropdown.append($('<option>').val(this.id).text(this.name));
                    });
                }
            });
        });
    </script>*@
}
